# 障害記録 2026-07-23

## 失敗したステップ

手順1: `pnpm status:selfcheck`

## 結果

終了コード: 2（疎通・認証NG）

## エラーメッセージ

```
ERROR: 環境変数 OKINA_STATUS_TOKEN が未設定です。Routines環境変数またはローカルの .env を確認してください
```

## 対応

`OKINA_STATUS_TOKEN` が未設定のため、以降のステップ（snapshot取得・レポート作成・publish・notify）は実行不能。
Routines環境変数またはローカルの `.env` に `OKINA_STATUS_TOKEN` を設定のうえ、再実行が必要。
